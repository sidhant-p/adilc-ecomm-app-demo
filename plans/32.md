# Implement authentication API and user persistence Implementation Plan

For agentic workers: run `/sdlc:dev-implement 32`

**Goal:** Deliver FastAPI authentication endpoints and SQLite-backed user persistence for registration, login, token-authenticated profile lookup, and stateless logout.

**Architecture:** Add a backend service under `backend/` with SQLAlchemy models, Pydantic schemas, JWT utilities, and an `auth` router mounted in `main.py`. Keep auth stateless by issuing signed JWT access tokens (no server-side token store) and deriving user context from bearer tokens on each request. Validate behavior with Node.js contract tests that execute against the running FastAPI app so acceptance criteria are checked from the HTTP boundary.

**Tech Stack:** Python 3.12, FastAPI, SQLAlchemy, SQLite, Pydantic, passlib[bcrypt], python-jose, Uvicorn, Node.js test runner (`node --test`).

## Global Constraints

- JWT: HS256, 30-minute expiry, secret from `.env`
- Passwords: hashed with `bcrypt` via `passlib`
- Passwords never stored plaintext — bcrypt only.
- JWT secret in `.env`, never committed.
- `.env` and `ecomm.db` in `../.gitignore`.
- CORS restricted to `http://localhost:5173` in development.

### Task 1: Create backend auth foundation

**Files:**
- **Create:** `backend/main.py`
- **Create:** `backend/database.py`
- **Create:** `backend/models.py`
- **Create:** `backend/schemas.py`
- **Create:** `backend/auth.py`
- **Create:** `backend/routers/auth.py`
- **Create:** `backend/routers/__init__.py`
- **Create:** `backend/requirements.txt`
- **Create:** `backend/.env.example`
- **Create:** `backend/tests/helpers/server.mjs`
- **Create:** `backend/tests/auth.foundation.test.mjs`
- **Modify:** `.gitignore`

**Interfaces:**
- **Consumes:**
  - `os.getenv("JWT_SECRET")` from `.env`
  - `DATABASE_URL` env var (default `sqlite:///./backend/ecomm.db`)
- **Produces:**
  - `def get_db() -> Generator[Session, None, None]` in `backend/database.py`
  - `class User(Base)` with columns `(id, email, hashed_password, full_name, created_at)` in `backend/models.py`
  - `def hash_password(password: str) -> str` in `backend/auth.py`
  - `def verify_password(plain_password: str, hashed_password: str) -> bool` in `backend/auth.py`
  - `def create_access_token(subject: str) -> str` in `backend/auth.py`
  - `def decode_access_token(token: str) -> dict` in `backend/auth.py`
  - `app = FastAPI(...)` with `app.include_router(auth_router, prefix="/auth")` in `backend/main.py`

- [ ] Write failing foundation test in `backend/tests/auth.foundation.test.mjs` that starts the app and asserts:
  - `POST /auth/logout` exists and returns non-404
  - User table schema includes `email` unique and `hashed_password` field
  - `.env` secret is required for token creation (requesting token without secret returns server error at startup)
  Use this concrete test scaffold:
  ```js
  import test from "node:test";
  import assert from "node:assert/strict";
  import { startServer, stopServer, requestJson } from "./helpers/server.mjs";

  test("auth foundations are wired", async () => {
    const server = await startServer();
    const logout = await requestJson(server, "POST", "/auth/logout");
    assert.notEqual(logout.status, 404);
    await stopServer(server);
  });
  ```
- [ ] Run and confirm failure: `node --test backend/tests/auth.foundation.test.mjs`  
  Expected: at least one failing assertion because backend files/endpoints are not implemented yet.
- [ ] Implement minimal foundation files and wiring:
  - Configure SQLAlchemy engine/session in `database.py`.
  - Define `User` model with unique email index.
  - Add password hash/verify and JWT helper functions in `auth.py`.
  - Mount `/auth` router in `main.py`.
  - Add `.env.example` with `JWT_SECRET=change-me`.
  - Update `.gitignore` to include `backend/ecomm.db` and `backend/.env`.
- [ ] Re-run and confirm pass: `node --test backend/tests/auth.foundation.test.mjs`  
  Expected: `ok 1 - auth foundations are wired`.
- [ ] Commit with message: `feat(auth): scaffold backend auth foundation`.

### Task 2: Implement registration endpoint

**Files:**
- **Modify:** `backend/routers/auth.py`
- **Modify:** `backend/schemas.py`
- **Modify:** `backend/models.py`
- **Create:** `backend/tests/auth.register.test.mjs`

**Interfaces:**
- **Consumes:**
  - `hash_password(password: str) -> str`
  - `get_db() -> Generator[Session, None, None]`
- **Produces:**
  - `class RegisterRequest(BaseModel): full_name: str; email: EmailStr; password: str`
  - `class RegisterResponse(BaseModel): id: int; email: EmailStr; full_name: str`
  - `@router.post("/register", status_code=201, response_model=RegisterResponse)`

- [ ] Write failing test `backend/tests/auth.register.test.mjs` with exact cases:
  1. Register new user returns `201` and response includes `id`, `email`, `full_name`.
  2. Duplicate email returns `409` and JSON body has `detail`.
  3. Stored record does not contain plaintext password (query DB and assert `hashed_password !== input password`).
  Use this request payload:
  ```json
  { "full_name": "Test User", "email": "test@example.com", "password": "S3curePass!" }
  ```
- [ ] Run and confirm failure: `node --test backend/tests/auth.register.test.mjs`  
  Expected: endpoint/status assertions fail before implementation.
- [ ] Implement minimal registration flow:
  - Validate request via `RegisterRequest`.
  - Query by email before insert.
  - On duplicate, raise `HTTPException(status_code=409, detail="Email already registered")`.
  - Hash password with bcrypt helper and persist `hashed_password`.
  - Return response without password fields.
- [ ] Re-run and confirm pass: `node --test backend/tests/auth.register.test.mjs`  
  Expected: all 3 register cases pass.
- [ ] Commit with message: `feat(auth): add register endpoint with duplicate protection`.

### Task 3: Implement login and /me token-auth flow

**Files:**
- **Modify:** `backend/routers/auth.py`
- **Modify:** `backend/auth.py`
- **Modify:** `backend/schemas.py`
- **Create:** `backend/tests/auth.login-me.test.mjs`

**Interfaces:**
- **Consumes:**
  - `verify_password(plain_password: str, hashed_password: str) -> bool`
  - `create_access_token(subject: str) -> str`
  - `decode_access_token(token: str) -> dict`
- **Produces:**
  - `class LoginRequest(BaseModel): email: EmailStr; password: str`
  - `class LoginResponse(BaseModel): access_token: str; token_type: Literal["bearer"]`
  - `def get_current_user(credentials: HTTPAuthorizationCredentials = Depends(security), db: Session = Depends(get_db)) -> User`
  - `@router.post("/login", response_model=LoginResponse)`
  - `@router.get("/me", response_model=RegisterResponse)`

- [ ] Write failing test `backend/tests/auth.login-me.test.mjs` with exact checks:
  1. Valid credentials return `200` with JWT `access_token`.
  2. Invalid password returns `401` with `detail`.
  3. Expired token returns `401` with `detail`.
  4. Valid bearer token on `GET /auth/me` returns user identity.
  5. JWT header `alg` is `HS256` and payload expiry (`exp`) is within 30 minutes of issuance.
- [ ] Run and confirm failure: `node --test backend/tests/auth.login-me.test.mjs`  
  Expected: login and token checks fail before handlers/dependency are implemented.
- [ ] Implement minimal login + auth guard:
  - Verify credentials against stored user hash.
  - Return `401` for unknown email or bad password.
  - Generate token with `sub=email`, `exp=now+30m`, signed with HS256 secret from `.env`.
  - Implement bearer-token dependency for `/auth/me`; reject invalid/expired token with `401`.
  - Return only current user info for `/auth/me`.
- [ ] Re-run and confirm pass: `node --test backend/tests/auth.login-me.test.mjs`  
  Expected: all login and `/me` auth cases pass.
- [ ] Commit with message: `feat(auth): add login and authenticated me endpoint`.

### Task 4: Finalize stateless logout and acceptance sweep

**Files:**
- **Modify:** `backend/routers/auth.py`
- **Create:** `backend/tests/auth.logout.test.mjs`
- **Create:** `backend/tests/auth.acceptance.test.mjs`

**Interfaces:**
- **Consumes:**
  - Existing FastAPI router and auth dependency wiring from Tasks 1-3
- **Produces:**
  - `@router.post("/logout")` returning `{"detail":"Logged out successfully"}` with HTTP `200`
  - No token blacklist or server-side session store introduced

- [ ] Write failing tests:
  - `backend/tests/auth.logout.test.mjs` asserts `POST /auth/logout` returns `200` and explicit success JSON.
  - `backend/tests/auth.acceptance.test.mjs` runs end-to-end flow: register → login → me → logout and verifies error semantics (`409` duplicate, `401` invalid/expired token).
- [ ] Run and confirm failure: `node --test backend/tests/auth.logout.test.mjs backend/tests/auth.acceptance.test.mjs`  
  Expected: at least one assertion fails until logout payload and final error behavior are wired.
- [ ] Implement minimal logout response (stateless only):
  - Add route returning explicit success payload.
  - Do not add persistence or revocation table.
  - Ensure all auth-related error responses use `{ "detail": "..." }`.
- [ ] Re-run and confirm pass: `node --test backend/tests/auth.logout.test.mjs backend/tests/auth.acceptance.test.mjs`  
  Expected: logout and end-to-end acceptance tests pass.
- [ ] Commit with message: `feat(auth): finalize stateless logout and auth acceptance coverage`.

## Dependencies & Out of Scope

- Story #33 (Build registration and login pages, issue #33) is out of scope: this plan covers backend APIs and persistence only.
- Story #34 (Protect dashboard and session flow, issue #34) is out of scope: frontend route guards and token UX are implemented in a separate story.
- Story #35 (Harden auth module security configuration, issue #35) is out of scope: production hardening and deeper security controls are tracked separately.
- Password reset, social/OAuth login, email verification, and role-based access control are out of scope per the approved spec.
- Production deployment configuration is out of scope per the approved spec.
